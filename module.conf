ABK_MODULE_ID="abk_5_15_backport"
ABK_MODULE_NAME="ABK 5.15 LTS Backport"
ABK_MODULE_VERSION="0.5.0"
ABK_MODULE_DESCRIPTION="Standalone feature, optimization, and structural-refactor backport line for the android13-5.15 GKI baseline (5.15.167), grafting selected upstream 5.15.y commits (v5.15.167..v5.15.218) plus the android14-6.1 ACK line (memory.reclaim, PSI IRQ/kernfs polling, lazy-preemption and lock-wakeup hooks) as bounded anchor scripts: per-group shape preflight, transactional replace steps, and report-only degradation instead of half-patched trees; new exported-struct fields only reuse free ANDROID_KABI_RESERVE slots. Security-only fixes are out of scope. Composable with other ABK modules via documented injection order (docs/porting_policy.md)."
ABK_MODULE_REPO_URL="https://github.com/xingguangcuican6666/ABK_5.15_backport.git"
ABK_MODULE_SUPPORTED_STAGES="after_patch,before_build"
ABK_MODULE_DEFAULT_STAGE="after_patch"
ABK_MODULE_RECOMMENDED_STAGES="after_patch"

ABK_MODULE_KIND="module_set"
ABK_MODULE_SET_ID="abk_5_15_backport"
ABK_MODULE_SET_NAME="ABK 5.15 LTS Backport"
ABK_MODULE_SET_VERSION="0.5.0"
ABK_MODULE_SET_DESCRIPTION="Feature/optimization backport line for android13-5.15: stable_backport_core carries the fd table conventions, page_alloc reserve/THP semantics, cgroup grafts, and per-memcg proactive reclaim; stable_perf_backport carries the scheduler NOHZ/PSI/RT, kstack randomization, networking, locking, block PM, OOM observability, and the android14-6.1 PSI / lazy-preemption / lock-wakeup grafts."

# child_id|name|description|repo_url|supported_stages|default_stage|recommended_stages|group_role|controllable|has_web_ui|magisk_module_name|magisk_module_url
ABK_MODULE_SET_ITEMS='
stable_backport_core|ABK 5.15 Backport Core|Port the upstream 5.15.y fd-table allocation conventions (5.15.191), the page_alloc reserve-semantics rework, the __GFP_THISNODE THP no-reclaim change, the cgroup root_list RCU / destroy-wq split, the android14-6.1 per-memcg memory.reclaim proactive-reclaim interface, and the android15-6.6 zram multi-compression recompression (ZRAM_MULTI_COMP + ZRAM_TRACK_ENTRY_ACTIME) onto the 5.15.167 GKI baseline; leaves the tree in the upstream fdtable shape that ABK_ABI_PATCH_SUITE fd_alloc_hotpath detects and adapts to.|https://github.com/xingguangcuican6666/ABK_5.15_backport.git|after_patch|after_patch|after_patch|feature|false|false||
stable_perf_backport|ABK 5.15 Backport Perf|Port the upstream 5.15.y scheduler NOHZ idle-balance series, PSI migration flags, RT scan optimizations, per-task kstack randomization, __release_sock cond_resched reduction, semaphore wake_q, blk-mq suspend wakeup abort, and mark_victim tracepoint fields, plus the android14-6.1 line (PSI IRQ pressure, PSI trigger kernfs polling, lazy-preemption and mutex/rwsem wakeup vendor hooks); every group degrades to a reported status when its anchor shape is absent.|https://github.com/xingguangcuican6666/ABK_5.15_backport.git|after_patch|after_patch|after_patch|feature|false|false||
'
